import styled from "@emotion/styled";

const Container = styled.div``;

interface Props {}

const CalendarExportToPdfButton = ({}: Props) => {
  return <Container></Container>;
};

export default CalendarExportToPdfButton;
