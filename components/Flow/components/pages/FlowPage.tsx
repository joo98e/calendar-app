import styled from "@emotion/styled";
import FlowTemplate from "@components/Flow/components/templates/FlowTemplate";

const Container = styled.div``;

interface Props {}

const FlowPage = ({}: Props) => {
  return <FlowTemplate />;
};

export default FlowPage;
